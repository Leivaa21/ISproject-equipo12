#include "persona.h"


Persona::Persona(string nombre, string apellido1, string apellido2, string dni, string fecha){
    setNombre(nombre);
    setApellido1(apellido1);
    setApellido2(apellido2);
    setDni(dni);
    setFecha(fecha);
}